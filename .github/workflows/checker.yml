name: Branch Checker

on:
    pull_request:
      branches: [ dev ]

jobs:
  checker-job:
    runs-on: ubuntu-latest
    steps:
        - if: "!startsWith(github.ref, 'refs/release/')"
          run: exit 1
